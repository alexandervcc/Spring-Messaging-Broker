spring:
  rabbitmq:
    host: localhost
    port: 5673
    username: rabbit
    password: rabbit
    
    #default values for exchange & routing-key
    template:
      exchange: memo.queue
      routing-key: memo.info
